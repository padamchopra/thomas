{
  "name": "thomas",
  "version": "0.0.1",
  "description": "A local-first CLI for managing agent workspaces with git worktrees.",
  "bin": {
    "thomas": "./bin/thomas.js"
  },
  "scripts": {
    "check": "node --check bin/thomas.js",
    "test": "node --test"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
