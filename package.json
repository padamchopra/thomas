{
  "name": "conductor-cli",
  "version": "0.1.0",
  "description": "A local-first CLI for managing agent workspaces with git worktrees.",
  "bin": {
    "conductor-cli": "./bin/conductor-cli.js"
  },
  "scripts": {
    "check": "node --check bin/conductor-cli.js",
    "test": "node --test",
    "build:macos-menu": "./macos/ConductorMenu/build-app.sh"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
